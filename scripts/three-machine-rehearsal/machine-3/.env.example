# Machine 3: participant-02, witness-02, mirror-02, auditor-02, release signer.
# Kit setup creates .env with release fields filled; replace the remaining placeholders.

RELAY_TAG=REPLACE_WITH_APPROVED_RELAY_TAG
MPC_RELEASE_REPOSITORY=REPLACE_WITH_APPROVED_OWNER_AND_REPOSITORY
MPC_TAG=REPLACE_WITH_APPROVED_MPC_TAG
RELAY_SHA256=REPLACE_WITH_64_CHARACTER_RELAY_SHA256
MPC_SHA256=REPLACE_WITH_64_CHARACTER_MPC_SHA256
RELAY_BIN=/usr/local/bin/relay
MPC_BIN=/usr/local/bin/mpc-ceremony

# Ceremony-kit setup replaces WORK_ROOT with REHEARSAL_ROOT/work/machine-3.
# When using this template without the kit, replace it manually. Place this
# machine's public material and private keys in the derived directories
# documented in the rehearsal README. The coordinator public key must still
# arrive through the independent trust channel.
WORK_ROOT=/absolute/path/to/machine-3-work
CEREMONY_ROOT=$WORK_ROOT/public
CONFIG_ROOT=$WORK_ROOT/config
KEYS_ROOT=$WORK_ROOT/keys
RUN_ROOT=$WORK_ROOT/run
TRUSTED_COORDINATOR_KEY=$WORK_ROOT/trust/coordinator-public-key.hex
STORAGE_CONFIG=$WORK_ROOT/trust/relay-storage.json

# The published bucket and anonymous HTTPS origin are read from the handed-off
# relay-storage.json instead of being repeated here. No long-lived reader
# credential is needed.
